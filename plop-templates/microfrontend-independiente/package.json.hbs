{
  "name": "{{kebabCase name}}",
  "version": "1.0.0",
  "description": "{{description}}",
  "type": "module",
    "scripts": {
       "dev": "npm run format && npm run lint && vite --host",
       "build": "npm run lint && vue-tsc -b && vite build",
       "builddev": "npm run lint && vue-tsc -b && vite build --mode development",
       "lint": "eslint . --report-unused-disable-directives --max-warnings 0",
       "format": "prettier --write ."
    },
    "dependencies": {
         "@pnpmworkspace/types": "workspace:*",
         "@pnpmworkspace/vue-utils": "workspace:*",
         "i18next": "catalog:",
         "i18next-http-backend": "catalog:",
         "i18next-vue": "catalog:",
         "pinia": "catalog:",
         "vue": "catalog:",
    },
    "devDependencies": {}
}